<UserControl x:Class="projekt2wpf.BookDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:projekt2wpf">

    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled"
                  Background="#FAFAFA">
        <Grid Margin="28,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
                <Button Content="← Back"
                        Command="{Binding BackCommand}"
                        Padding="12,6" Margin="0,0,8,0"
                        Background="{StaticResource PrimaryBrush}"
                        Foreground="White" BorderBrush="{StaticResource PrimaryBrush}"
                        Cursor="Hand"/>
                <Button Content="✏ Edit"
                        Command="{Binding EditCommand}"
                        Padding="12,6"
                        Background="White" Foreground="{StaticResource PrimaryBrush}"
                        BorderBrush="{StaticResource PrimaryBrush}" BorderThickness="1.5"
                        Cursor="Hand"/>
            </StackPanel>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0"
                        Width="180" Height="240"
                        Background="#E4E4E4" CornerRadius="6"
                        ClipToBounds="True" Margin="0,0,28,0"
                        VerticalAlignment="Top">
                    <Grid>
                        <TextBlock Text="No Cover"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Foreground="#999" FontSize="13"/>
                        <Image Source="{Binding Book.CoverImage, Converter={StaticResource ByteToImageConverter}}"
                               Stretch="UniformToFill"/>
                    </Grid>
                </Border>

                <StackPanel Grid.Column="1" VerticalAlignment="Top">

                    <TextBlock Text="{Binding Book.Title}"
                               FontSize="26" FontWeight="Bold"
                               TextWrapping="Wrap" Margin="0,0,0,4"/>

                    <TextBlock Text="{Binding Book.Author}"
                               FontSize="16" Foreground="#666666"
                               Margin="0,0,0,8"/>

                    <local:RatingControl Value="{Binding Book.Rating, TargetNullValue=0}"
                                         IsReadOnly="True" StarSize="20"
                                         Margin="0,0,0,6"
                                         Visibility="{Binding Book.Rating, Converter={StaticResource NullToVisibilityConverter}}"/>

                    <TextBlock Text="{Binding StatusText}"
                               FontSize="12" Foreground="White"
                               Background="{StaticResource AccentBrush}"
                               Padding="8,3" Margin="0,0,0,14"
                               HorizontalAlignment="Left"/>

                    <StackPanel Visibility="{Binding HasDescription, Converter={StaticResource BoolToVisibilityConverter}}"
                                Margin="0,0,0,16">
                        <TextBlock Text="Description"
                                   FontWeight="SemiBold" FontSize="13"
                                   Foreground="{StaticResource PrimaryBrush}"
                                   Margin="0,0,0,6"/>
                        <TextBlock Text="{Binding Book.Description}"
                                   TextWrapping="Wrap" FontSize="13"
                                   Foreground="#444444" LineHeight="20"/>
                    </StackPanel>

                    <StackPanel Visibility="{Binding HasTags, Converter={StaticResource BoolToVisibilityConverter}}"
                                Margin="0,0,0,16">
                        <TextBlock Text="Tags"
                                   FontWeight="SemiBold" FontSize="13"
                                   Foreground="{StaticResource PrimaryBrush}"
                                   Margin="0,0,0,6"/>
                        <local:TagControl Tags="{Binding Book.Tags}"
                                          IsEditable="False"/>
                    </StackPanel>

                    <StackPanel Visibility="{Binding HasPages, Converter={StaticResource BoolToVisibilityConverter}}"
                                Margin="0,0,0,16">
                        <TextBlock Text="Reading Progress"
                                   FontWeight="SemiBold" FontSize="13"
                                   Foreground="{StaticResource PrimaryBrush}"
                                   Margin="0,0,0,6"/>
                        <ProgressBar Value="{Binding ProgressPct, Mode=OneWay}" Maximum="100"
                                     Height="8"
                                     Foreground="{StaticResource AccentBrush}"
                                     Background="#E0E0E0"
                                     Margin="0,0,0,4"/>
                        <TextBlock Text="{Binding ProgressText}"
                                   FontSize="12" Foreground="#666"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <Button Content="📖  Read from Start"
                                Command="{Binding ReadFromStartCommand}"
                                Visibility="{Binding HasFile, Converter={StaticResource BoolToVisibilityConverter}}"
                                Padding="14,8" Margin="0,0,10,0"
                                Background="{StaticResource AccentBrush}"
                                Foreground="White" BorderBrush="{StaticResource AccentBrush}"
                                Cursor="Hand"/>
                        <Button Content="▶  Continue Reading"
                                Command="{Binding ContinueReadingCommand}"
                                Visibility="{Binding CanContinue, Converter={StaticResource BoolToVisibilityConverter}}"
                                Padding="14,8"
                                Background="White" Foreground="{StaticResource AccentBrush}"
                                BorderBrush="{StaticResource AccentBrush}" BorderThickness="1.5"
                                Cursor="Hand"/>
                    </StackPanel>

                </StackPanel>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>
